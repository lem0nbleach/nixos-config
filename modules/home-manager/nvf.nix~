{
  programs.nvf = {
    enable = true;
    settings = {
      vim = {
      
        languages.nix = {
	  enable = true;
	  format.enable = true;
	  lsp.enable = true;
	};

        filetree = {
          eno-tree = {
            enable = true;
          };
        };

        spellcheck = {
          enable = true;
        };

        git = {
          enable = true;
          gitsigns.enable = true;
        };
        
        statusline = {
          lualine = {
            enable = true;
          };
        };
      };
    };
  };
}
